See `binary_analysis/skills/README.md`. Global skills are under `/skills/`.

Bundle-local skill packages for official agents live under each bundle's `skills/` directory when the backend mounts per-bundle skill roots.
