# LangGraph DeepAgent Service Dependencies
# Install with: pip install -r requirements.txt
# Phase 1.1: Upgraded for official deepagents migration (langchain 1.2+, deepagents 0.4+)

# Core LangChain & LangGraph - Based on official DeepAgents framework
langchain>=1.2.10,<2.0.0
langchain-core>=1.2.10,<2.0.0
langgraph>=0.2.0
langgraph-checkpoint-postgres>=2.0.0  # PostgreSQL checkpointing; verify compat with langchain 1.2+

# Official DeepAgents (create_deep_agent, SubAgentMiddleware, etc.)
deepagents>=0.4.3

# LLM Providers
langchain-openai>=0.2.0
langchain-anthropic>=1.3.3,<2.0.0
langchain-google-genai>=4.2.0,<5.0.0

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17
pyjwt>=2.8.0

# Async HTTP
httpx>=0.28.0
aiohttp>=3.11.0

# Environment & Config
python-dotenv>=1.0.0
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0

# Supabase client (optional - for cloud database mode)
supabase>=2.10.0

# Stripe (billing checkout, portal, webhooks)
stripe>=11.0.0,<13.0.0

# PostgreSQL (for local database mode)
asyncpg>=0.30.0
psycopg2-binary>=2.9.9
psycopg-binary>=3.2.0  # Required for langgraph-checkpoint-postgres on Windows

# Utilities
tenacity>=9.0.0
structlog>=24.4.0
wcmatch>=8.0  # Glob matching for vendored deepagents backends
charset-normalizer>=3.3.0

# -----------------------------------------------------------------------------
# Official bundle parity: subagents/official/binary_analysis/pyproject.toml
#
# Core [project] deps are already covered above (deepagents, pydantic*, dotenv,
# langchain-google-genai). Only bundle-specific imports need an explicit line here:
# -----------------------------------------------------------------------------
python-ulid>=3  # Indicator.id; imported by schema.indicator, file_identify, document_extract

# [documents] optional-dependencies — same host Python runs sandbox/document_workers/*.py
# when BINARY_ANALYSIS uses subprocess backend (default local dev). E2B-only deployments
# may rely on the template image instead; keep these for parity with the bundle wheel.
oletools>=0.60
msoffcrypto-tool>=5.0
pypdf>=4.0
pdfminer.six>=20231228
XLMMacroDeobfuscator>=0.2.0
# Dev-only (langgraph dev / deep-agents-ui): langgraph-cli[inmem]>=0.1.55

# Debugging support for VS Code
debugpy>=1.8.0

# AI Gateway dependencies
pyyaml>=6.0.1
cryptography>=42.0.0

# File type detection
python-magic-bin>=0.4.14; platform_system == "Windows"
python-magic>=0.4.27; platform_system != "Windows"

# Executable file parsing
pefile>=2023.2.7
pyelftools>=0.31

# Network packet parsing
scapy>=2.5.0

# Archive files
py7zr>=0.21.0
rarfile>=4.1

# Document parsing
PyPDF2>=3.0.0
python-docx>=1.1.0

# HTML parsing for fallback scraping
beautifulsoup4>=4.12.0

# OCR
pytesseract>=0.3.10
Pillow>=10.0.0

# Web threat L1 YARA (detect_web_attack)
yara-python>=4.3.1

# E2B cloud sandbox (on-demand dynamic analysis, malicious URL simulation, etc.)
e2b>=1.0.0

# QR decode (email-security / quishing tooling)
pyzbar>=0.1.9

# CI / tests (.github workflows run install + test from .cicd/project.yaml)
pytest>=7.0.0
pytest-asyncio>=0.24.0
