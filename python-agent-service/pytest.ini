[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    e2e: End-to-end tests (require LLM API key, skip in CI)
    integration: Integration tests (may call external APIs)