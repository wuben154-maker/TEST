# Copy to `.env.production.ec2` (keep out of Git) before VM deploy builds.
VITE_API_MODE=local
# Must be reachable from browsers (public EC2 DNS or elastic IP).
VITE_LOCAL_API_URL=http://YOUR_PUBLIC_EC2_HOST:8000
