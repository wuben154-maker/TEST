# Moved

UI reference screenshots now live under each requirement folder:

**`docs/Process/<requirement-slug>/mockups/`**

See **`docs/Process/_templates/ACCEPTANCE_UI_SPEC.md`** and **`delivery-pipeline`** skill.

Do not add new assets in this `.cursor/.../mockups/` directory.
